const Trade = require('../models/tradeModel');
const Stock = require('../models/stockModel');

exports.buyStock = async (stockId, quantity) => {
  
};

exports.sellStock = async (stockId, quantity) => {
 
};
